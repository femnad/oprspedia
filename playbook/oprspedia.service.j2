[Unit]
Description=Oprspedia

[Service]
PIDFile={{ settings.pidfile }}
ExecStart=/usr/bin/thin -R {{ source.path }}/src/oprspedia.ru -C {{ local_var_dir }}/config.yaml -l {{ local_var_dir }}/oprs.log -P {{ settings.pidfile }} start
Restart=always

[Install]
WantedBy=multi-user.target
